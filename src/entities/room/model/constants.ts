import { PlankConfig, SkirtingConfig } from './types';

export const DEFAULT_PLANK: PlankConfig = {
  width: 0.1,
  length: 0.6,
  thickness: 0.01,
};

export const DEFAULT_SKIRTING: SkirtingConfig = {
  height: 0.1,
  thickness: 0.015,
  color: '#eeeeee',
};
